version: '3.9'

volumes:
  pgdata:

services:

  postgre:
    image: postgres:latest
    environment:
      POSTGRES_DB: "orders"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "Tehn89tehn"
    ports:
      - "5431:5432"